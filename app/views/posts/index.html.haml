%h1 Listing posts
%table
  %thead
    %tr
      %th Title
      %th Content
      %th
      %th
      %th
  %tbody
    - @posts.each do |post|
      %tr
        %td= post.title
        %td= post.post
        %td= link_to 'Show', post