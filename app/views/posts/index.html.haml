%h1 Listing posts
%table
  %thead
    %tr
      %th Title
      %th Content
      %th
      %th
      %th
  %tbody
    - @posts.each do |post|
      %tr
        %td= post.title
        %td= truncate(post.post, :length => 100)
        %td= link_to 'Show', post